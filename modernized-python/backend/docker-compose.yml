services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: orderco
      POSTGRES_PASSWORD: orderco
      POSTGRES_DB: orderco
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
